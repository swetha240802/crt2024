#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

       int n,d,s=0;
    scanf("%d",&n);
    while(n>0)
    {
        d=n%10;
        n=n/10;
    if(d%2!=0)
        s=s+d;
    }
    printf("%d\n",s);
    return 0;
}
